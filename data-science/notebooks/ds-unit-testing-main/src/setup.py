from setuptools import setup

setup(name='example',
      version='0.1',
      description='A example function that adds one to the input',
      url='http://github.com/example',
      author='Flying Circus',
      author_email='flyingcircus@example.com',
      license='MIT',
      packages=['example'],
      zip_safe=False)